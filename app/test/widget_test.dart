import 'package:flutter_test/flutter_test.dart';
import 'package:surface_sensor/main.dart';

void main() {
  testWidgets('SurfaceSensorApp can be constructed', (_) async {
    expect(const SurfaceSensorApp(), isA<SurfaceSensorApp>());
  });
}
